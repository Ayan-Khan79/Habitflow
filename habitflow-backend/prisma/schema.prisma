generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        Int      @id @default(autoincrement())
  name      String
  email     String   @unique
  password  String
  habits    Habit[]  @relation("UserHabits") // relation name added
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Habit {
  id             Int           @id @default(autoincrement())
  userId         Int
  user           User          @relation("UserHabits", fields: [userId], references: [id]) // relation to User
  title          String
  description    String
  frequency      String
  tags           String[]      @default([])
  completedCount Int           @default(0)
  longestStreak  Int           @default(0)
  reminderTime   DateTime?
  trackingLogs   TrackingLog[]
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt
}

model TrackingLog {
  id        Int      @id @default(autoincrement())
  habit     Habit    @relation(fields: [habitId], references: [id])
  habitId   Int
  date      DateTime
  createdAt DateTime @default(now())

  @@unique([habitId, date])
}

enum Frequency {
  daily
  weekly
}
